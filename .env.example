# Claude API - 基础配置
ANTHROPIC_API_KEY=sk-ant-xxx

# 📌 自定义模型配置（可选）
# 如果使用非 Anthropic 官方模型（如第三方代理、OpenAI 兼容 API 等），可以配置以下变量：

# 自定义 API 端点 URL（不使用 Anthropic 官方 API 时设置）
# 示例: https://api.openai.com/v1 或自定义代理地址
ANTHROPIC_BASE_URL=

# 自定义模型名称（不使用 Anthropic 官方模型时设置）
# 示例: gpt-4o, claude-3.5-sonnet, deepseek-chat 等
ANTHROPIC_MODEL=

# ============================================
# 邮件服务配置
# ============================================

# 方式1：使用 Resend API（推荐，简单配置）
RESEND_API_KEY=re_xxx
RESEND_FROM_EMAIL=noreply@yourdomain.com
RESEND_TO_EMAIL=recipient@example.com

# 方式2：使用 SMTP（如 163、QQ 邮箱）
# 启用 SMTP：设置 USE_SMTP=true
USE_SMTP=true

# SMTP 服务器配置（当 USE_SMTP=true 时使用）
# 以下配置项应该从 GitHub Secrets 中读取，而不是直接写在这里
# ⚠️ 安全提醒：不要将真实的 SMTP_USER 和 SMTP_PASSWORD 提交到代码仓库！
# 
# 在 GitHub 仓库 Settings → Secrets and variables → Actions 中配置：
# - SMTP_HOST
# - SMTP_PORT
# - SMTP_SECURE
# - SMTP_USER
# - SMTP_PASSWORD
# 
SMTP_HOST=smtp.163.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@163.com
SMTP_PASSWORD=your-smtp-password

# 注意：
# - SMTP_PASSWORD 是邮箱的 SMTP 授权码，不是邮箱登录密码
# - 163 邮箱需要在设置中开启 SMTP 服务并生成授权码
# - SMTP_SECURE=true 表示使用 SSL/TLS（465/587 端口），false 表示普通连接（25 端口）
# - 发件人邮箱必须与 SMTP_USER 一致

# GitHub Trending 语言配置
# 配置要抓取的语言列表，用逗号分隔
# 示例：typescript,python,go,rust,javascript
# 留空则抓取所有语言（默认）
TRENDING_LANGUAGES=

# 邮件发送控制（可选）
EMAIL_SEND_ENABLED=true  # 设为 false 则不发送邮件，改为输出内容
NODE_ENV=development     # 开发模式下自动不发送邮件

# 日志配置
LOG_LEVEL=info
# Claude 输出日志（可选）
# 设为 true 会在控制台打印大模型输出内容
LOG_LLM_OUTPUT=false
# Claude 流式输出（可选）
# 设为 true 会实时流式打印模型输出（需支持 SDK 流式事件）
LOG_LLM_STREAM=false
